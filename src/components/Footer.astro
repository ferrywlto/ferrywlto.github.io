---
const today = new Date();
---
<footer class="texture-overlay flex-center">
  <div class="footer-content">
    <span class="line-break">
    &copy; {today.getFullYear()} Ferry To & <a href="https://verdantsparks.io" target="_blank" class="link-accent">VerdantSparks</a>&trade;
    </span>
    <span class="line-break">All rights reserved.</span>
    <span class="line-break powered-by">       
      Powered by
      <a href="https://astro.build" target="_blank" class="link-accent">Astro</a> &
      <a href="https://alpinejs.dev" target="_blank" class="link-accent">Alpine.Js</a>
    </span>
  </div>
</footer>
<style>
	footer {
		box-sizing: border-box;
		height: var(--footer-height-desktop);
		max-height: var(--footer-height-desktop);
		overflow: hidden;
		padding: var(--space-md);
		color: var(--black-emperor);
		text-align: center;
    	background: transparent;
		box-shadow: var(--shadow-sm);
		border-top: 1px solid var(--black-opacity-05);
		transition: var(--transition-slow);
		/* Ensure footer styling is independent */
		font-family: inherit;
		font-size: var(--text-base);
		line-height: 1.4;
    	min-width: 280px;
	}

	.footer-content {
		position: relative;
		z-index: 2;
		max-width: var(--container-lg);
		margin: 0 auto;
	}

	footer a {
		font-weight: 500;
	}

	footer a:hover {
		text-decoration: underline;
	}

	.line-break {
		display: inline;
	}

	/* Responsive footer layout */
	@media (min-width: 471px) and (max-width: 1023px) {
		.line-break.powered-by {
			display: block;
      		margin-top: var(--space-sm);
		}
		footer {
			font-size: var(--text-sm);
		}
	}
  
	@media (max-width: 470px) {
		.line-break {
			display: block;
			margin-top: var(--space-xs);
		}
    
		.line-break:first-child {
			margin-top: 0;
		}

		footer {
			box-sizing: border-box;
			height: var(--footer-height);
			max-height: var(--footer-height);
			overflow: hidden;
			font-size: var(--text-xs);
		}
	}
</style>
