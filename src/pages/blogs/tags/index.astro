---
import TagCloud from "../../../layouts/TagCloud.astro";

const pageTitle = "Blog Tag Index";
const pageDescription = "A list of all blog tags";
const collection = "blogs";
const headerText = "All blog posts are grouped by the tags below:";
---

<TagCloud
  title={pageTitle}
  description={pageDescription}
  collection={collection}
  header={headerText}
/>
